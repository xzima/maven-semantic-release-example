diff --git a/node_modules/@saithodev/semantic-release-backmerge/dist/helpers/git.js b/node_modules/@saithodev/semantic-release-backmerge/dist/helpers/git.js
index d87c6eb..fd43de8 100644
--- a/node_modules/@saithodev/semantic-release-backmerge/dist/helpers/git.js
+++ b/node_modules/@saithodev/semantic-release-backmerge/dist/helpers/git.js
@@ -113,7 +113,7 @@ class Git {
      */
     checkout(branch) {
         return __awaiter(this, void 0, void 0, function* () {
-            yield execa('git', ['checkout', '-B', branch], this.execaOpts);
+            yield execa('git', ['checkout', '-B', branch, `origin/${branch}`], this.execaOpts);
         });
     }
     /**
@@ -155,6 +155,7 @@ class Git {
             const args = ['merge'];
             if (mergeMode !== 'none') {
                 args.push('-X' + mergeMode);
+                args.push('-mchore(release): Sync with next release [skip ci]');
             }
             args.push('origin/' + branch);
             yield execa('git', args, this.execaOpts);
diff --git a/node_modules/@saithodev/semantic-release-backmerge/dist/perform-backmerge.js b/node_modules/@saithodev/semantic-release-backmerge/dist/perform-backmerge.js
index 1810dea..e8800c1 100644
--- a/node_modules/@saithodev/semantic-release-backmerge/dist/perform-backmerge.js
+++ b/node_modules/@saithodev/semantic-release-backmerge/dist/perform-backmerge.js
@@ -31,6 +31,7 @@ function performBackmergeIntoBranch(git, pluginConfig, context, options, develop
             context.logger.log(`Performing backmerge with "${options.backmergeStrategy}" strategy.`);
             if (options.backmergeStrategy === 'merge') {
                 yield git.merge(releaseBranchName, options.mergeMode);
+                yield git.merge(developBranchName, options.mergeMode);
             }
             else {
                 try {
